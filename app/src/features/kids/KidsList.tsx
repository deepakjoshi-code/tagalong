import { EmptyState, Screen } from '@/design/components'

/** Placeholder — replaced by the feature build. */
export function KidsList() {
  return (
    <Screen title="Kids">
      <EmptyState title="Kids" message="Kid profiles are being built." />
    </Screen>
  )
}
